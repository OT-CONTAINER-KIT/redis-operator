---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-standalone-shrink-test-redis-standalone-shrink-test-0
  labels:
    app: redis-standalone-shrink-test
    redis_setup_type: standalone
    role: standalone
status:
  accessModes: [ReadWriteOnce]
  capacity:
    storage: 1Gi
  phase: Bound
