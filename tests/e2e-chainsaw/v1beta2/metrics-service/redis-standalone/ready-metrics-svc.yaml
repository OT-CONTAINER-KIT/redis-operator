---
apiVersion: v1
kind: Service
metadata:
  name: redis-standalone-metrics-metrics
  labels:
    app: redis-standalone-metrics
    redis_setup_type: standalone
    role: standalone
    app.kubernetes.io/component: metrics
  annotations:
    prometheus.io/port: "9121"
    prometheus.io/scrape: "true"
  ownerReferences:
    - apiVersion: redis.redis.opstreelabs.in/v1beta2
      controller: true
      kind: Redis
      name: redis-standalone-metrics
spec:
  type: ClusterIP
  ports:
    - name: redis-exporter
      port: 9121
      protocol: TCP
      targetPort: 9121
  selector:
    app: redis-standalone-metrics
    redis_setup_type: standalone
    role: standalone
